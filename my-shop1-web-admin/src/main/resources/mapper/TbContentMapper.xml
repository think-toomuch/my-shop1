<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.atpisher.my.shop1.web.admin.dao.TbContentDao">
    <sql id="tbContentColumns">
        a.id,
        a.category_id AS categoryId,
        a.title,
        a.sub_title AS subTitle,
        a.title_desc AS titleDesc,
        a.url,
        a.pic,
        a.pic2,
        a.content,
        a.created,
        a.updated
    </sql>

    <select id="selectAll" resultType="TbContent">
        SELECT
        <include refid="tbContentColumns"/>
        FROM
        tb_content AS a
    </select>

    <insert id="insert">
        INSERT INTO tb_content (
            id,
            category_id,
            title,
            sub_title,
            title_desc,
            url,
            pic,
            pic2,
            content,
            created,
            updated
        )
        VALUES
        (
            #{id},
            #{categoryId},
            #{title},
            #{subTitle},
            #{titleDesc},
            #{url},
            #{pic},
            #{pic2},
            #{content},
            #{created},
            #{updated}
        )
    </insert>

    <delete id="delete">
        DELETE FROM tb_content WHERE id = #{id}
    </delete>

    <update id="update">
        UPDATE
            tb_content
        SET
            category_id=#{categoryId},
            title=#{title},
            sub_title=#{subTitle},
            title_desc=#{titleDesc},
            url=#{url},
            pic=#{pic},
            pic2=#{pic2},
            content=#{content},
            created=#{created},
            updated=#{updated}
        WHERE id = #{id}
    </update>


    <select id="getById" resultType="TbContent" parameterType="java.lang.Long">
        select * from tb_content where id=#{id}
    </select>
    <!--    根据id删除多个内容-->
    <delete id="deleteTbContentsById">
        delete from tb_content
        where id in
        <foreach collection="array" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </delete>
    <!--分页查询指定条数-->
    <select id="page" resultType="TbContent" parameterType="java.util.Map">
        SELECT
        <include refid="tbContentColumns"/>
        FROM
        tb_content AS a
        <where>
            <if test="tbContent.title!=null and tbContent.title!=''">
                and a.title like concat('%',#{tbContent.title},'%')
            </if>
            <if test="tbContent.subTitle!=null and tbContent.subTitle!=''">
                and a.sub_title like concat('%',#{tbContent.subTitle},'%')
            </if>
            <if test="tbContent.titleDesc!=null and tbContent.titleDesc!=''">and a.title_desc like
                concat('%',#{tbContent.titleDesc},'%')
            </if>
        </where>
        LIMIT #{start},#{length}
    </select>
    <!--    查询总条数-->
    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM
        tb_content as a
        <where>
            <if test="title!=null and title!=''">
                and a.title like concat('%',#{title},'%')
            </if>
            <if test="subTitle!=null and subTitle!=''">
                and a.sub_Title like concat('%',#{subTitle},'%')
            </if>
            <if test="titleDesc!=null and titleDesc!=''">
                and a.title_desc like concat('%',#{titleDesc},'%')
            </if>
        </where>
    </select>
</mapper>